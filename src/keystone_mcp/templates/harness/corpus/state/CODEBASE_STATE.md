# CODEBASE_STATE

**Single source of truth** for what this codebase IS at the most
recent reconciliation. Filled by the `keystone_bootstrap` playbook and
refreshed by the `keystone_audit` flywheel.

## Last reconciled

`<YYYY-MM-DD by <agent or human>>`

## Languages and runtimes

- <e.g. Python 3.12, Node 22, Go 1.22>

## Frameworks and primary libraries

- <e.g. FastAPI 0.110, React 18, Django 5>

## Build / test / lint commands

```
<exact commands the agent runs to build, test, lint, type-check>
```

## Architecture summary

<One paragraph: top-level shape — services, packages, layers,
boundaries.>

## Entry points

- <where requests enter / processes start / scheduled jobs trigger>

## Key external integrations

- <APIs, databases, message queues, third-party services>

## Notable conventions

- <idioms unique to this codebase the agent should preserve>
